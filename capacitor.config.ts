import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.neelam.telecast',
  appName: 'Telecast',
  webDir: 'www',
  server: { androidScheme: 'https' }
};

export default config;
